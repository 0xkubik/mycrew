# Features — mycrew

<!-- The product's declarative feature state, one list for the whole product: the code is pulled
     toward it. `- [ ]` = not built yet, `- [x]` = built. Built features stay checked forever — a
     line is never deleted; the list only accumulates. The `F`-id is permanent — the specs point at
     features by it. -->
