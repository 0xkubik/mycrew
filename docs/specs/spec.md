# Spec — mycrew

<!-- Everything the product must be built to, one file for every sub-project. One `##` per feature,
     headed by that feature's id from docs/features/features.md, holding only what the feature's one
     line can't carry. Under a heading: short `-` points only — a constraint, a wish about the
     implementation, a piece of data, an edge case, or what counts as done. Never prose. -->
