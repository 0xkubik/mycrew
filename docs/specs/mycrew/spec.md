# Spec — mycrew

<!-- Everything this sub-project must be built to. One `##` per feature, headed by that feature's id
     from docs/features/features.md, holding only what the feature's one line can't carry. Under a
     heading: short `-` points only — a constraint, a wish about the implementation, a piece of data,
     an edge case, or what counts as done. Never prose. -->
