# Technical decisions — mycrew

<!-- Every settled technical choice and the reasoning that closed it, one file for the whole product,
     newest last. The machinery only: a language, a framework, a store, a protocol, a library, a way
     of running it, something deliberately not used. What the product does belongs to
     ../features.md, a rule of the product a person could notice to ../decisions.md, the shape itself
     to model.c4. The `T`-id is permanent. The heading is the decision; under it short `-` points
     only — why it won, what it was weighed against, where it stops working, what would overturn it.
     Never prose. A decision that no longer holds is marked superseded, never deleted. -->
