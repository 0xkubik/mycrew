# Technical decisions — <product name>

<!-- TECHNICAL DECISIONS — how the product is actually built: the settled choices of implementation
     and the reasoning that closed each one. The tree beside this file says what the shape IS; this
     file says WHY it is that and not the other thing.

     WHAT GOES HERE: a closed choice about the machinery — a language, a framework, a store, a
     protocol, a library, a way of running or deploying it, something deliberately not used. Anything
     a person reading the product would never see.

     WHAT DOES NOT: what the product does goes to ../product/features.md. A rule of the product a
     person outside could notice goes to ../product/decisions.md. The shape itself is model.c4 —
     point at a node here, never redraw it.

     SHAPE: `## T001 — <what was chosen, and over what where there was a real alternative>`. A
     heading that names a topic instead of a choice is a decision not yet made. Under it short `-`
     points only: why it won, what it was weighed against and what that would have cost, the known
     limit where it stops working, what would overturn it. Never prose. Heading and points together
     stay within 300 chars; what will not fit is two decisions. -->

## T001 — <what was chosen, and over what where there was a real alternative>

- <why this won>
- <what it was weighed against, and what that would have cost>
- <the known ceiling — where this stops working>
- <what would overturn it>

## T002 — <a decision serving one feature> (F004)

- <why, in the same short points>

## T003 — <a decision that no longer holds> — superseded by T007

- <its points stay exactly as they were written>
