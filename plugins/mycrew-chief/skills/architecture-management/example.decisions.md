# Technical decisions — <product name>

<!-- Template for docs/architecture/decisions.md at the product root — every settled technical choice
     and the reasoning that closed it, one file for the whole product, newest last. The machinery
     only: a language, a framework, a store, a protocol, a library, a way of running it, something
     deliberately not used. What the product does belongs to docs/features.md, a rule of the product
     a person could notice to docs/decisions.md, the shape itself to model.c4. The `T`-id is
     permanent. The heading is the decision; under it short `-` points only — why it won, what it was
     weighed against, where it stops working, what would overturn it. Never prose. A decision that no
     longer holds is marked superseded, never deleted. -->

## T001 — <what was chosen, and over what where there was a real alternative>

- <why this won>
- <what it was weighed against, and what that would have cost>
- <the known ceiling — where this stops working>
- <what would overturn it>

## T002 — <a decision serving one feature> (F004)

- <why, in the same short points>

## T003 — <a decision that no longer holds> — superseded by T007

- <its points stay exactly as they were written>
