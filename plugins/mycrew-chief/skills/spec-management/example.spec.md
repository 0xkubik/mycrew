# Spec — <sub-project name>

<!-- Template for docs/specs/<sub-project>/spec.md at the product root — everything this ONE
     sub-project must be built to. One file per sub-project, the folder named as the root CLAUDE.md's
     Sub-projects list names it. One `##` per feature, headed by that feature's id from
     docs/features/features.md, holding only this sub-project's part of it. -->

## 000 - <the feature's line, as it reads in features.md>

<what this sub-project must be built to for feature 000: mechanics, contracts and interfaces, data
 shapes, states, edge cases, what counts as done — the detail the feature's one line can't carry.
 Not a restatement of the feature, not the code's organization, not the architecture.>

## 004 - <another feature this sub-project carries part of>

<this sub-project's part only — the other repos' parts live in their own spec.md under the same id.>
