# Spec — <sub-project name>

<!-- Template for docs/specs/<sub-project>/spec.md at the product root — everything this ONE
     sub-project must be built to. One file per sub-project, the folder named as the root CLAUDE.md's
     Sub-projects list names it. One `##` per feature, headed by that feature's id from
     docs/features/features.md, holding only this sub-project's part of it. Under a heading: short
     `-` points only — a constraint, a wish about the implementation, a piece of data the one line
     can't carry. Never prose. -->

## F000 - <the feature's line, as it reads in features.md>

- <a constraint this sub-project must hold to>
- <a wish about the implementation — how it should be built, where it's not free>
- <a piece of data the feature's line can't carry: a contract, a shape, a limit, a state>
- <an edge case, or what counts as done>

## F004 - <another feature this sub-project carries part of>

- <this sub-project's part only — the other repos' parts live in their own spec.md under the same id>
