# Spec — <product name>

<!-- Template for docs/specs/spec.md at the product root — everything the product must be built to,
     one file for every sub-project. One `##` per feature, headed by that feature's id from
     docs/features/features.md, holding the whole feature whichever repos build it. Under a heading:
     short `-` points only — a constraint, a wish about the implementation, a piece of data the one
     line can't carry. Never prose. -->

## F000 - <the feature's line, as it reads in features.md>

- <a constraint the feature must hold to>
- <a wish about the implementation — how it should be built, where it's not free>
- <a piece of data the feature's line can't carry: a contract, a shape, a limit, a state>
- <an edge case, or what counts as done>

## F004 - <another feature>

- <a point naming a sub-project only where that is what the point is about>
