# Features — <product name>

<!-- Template for docs/features.md at the product root — the product's declarative feature state, one
     list for every sub-project: the code is pulled toward it. `[ ]` = not built yet, `[x]` = built.
     Built features stay checked forever — an entry is never deleted; the list only accumulates. The
     `F`-id is permanent, and everything else points at features by it. The heading is the feature in
     ≤200 chars; under it short `-` points only, holding what that line can't carry. Heading and
     points together stay within 300 chars — what will not fit is two features. Never prose, and
     never why a choice was made — that is docs/decisions.md. -->

## [ ] F001 — <feature the user affirmed — one line, ≤200 chars>

- <a constraint it must hold to>
- <a wish about the implementation — how it should be built, where it's not free>
- <a piece of data the line can't carry: a contract, a shape, a limit, a state>
- <an edge case, or what counts as done>

## [ ] F002 — <a feature whose line says all there is to say carries no points at all>

## [x] F003 — <a feature already built — stays here forever>

- <its points stay too>
