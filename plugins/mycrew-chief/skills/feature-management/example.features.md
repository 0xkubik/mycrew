# Features — <product name>

<!-- Template for docs/features/features.md at the product root — the product's declarative feature
     state, one list for every sub-project: the code is pulled toward it. `- [ ]` = not built yet,
     `- [x]` = built. Built features stay checked forever — a line is never deleted; the list only
     accumulates. The `F`-id is permanent — the specs point at features by it. -->

- [ ] F000 - <feature the user affirmed — one line, ≤200 chars>
- [ ] F001 - <next feature>
- [x] F002 - <a feature already built — stays here forever>
