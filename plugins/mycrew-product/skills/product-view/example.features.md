# Features — <product name>

<!-- FEATURES — the functionality the product must have.

     WHAT GOES HERE: every capability the product is meant to do, built or not. One LINE per
     capability, one list for the whole product however many sub-projects build it.

     WHAT DOES NOT: detail of any kind — a constraint, a contract, a state, an edge case, mechanics.
     That lives in the feature's own file under features/, never in this list. Work someone has to do
     goes to notes.md, a settled rule about the product to decisions.md, how it is built to ../design/.

     SHAPE: `- [ ] F001 — <the feature, one line, ≤200 chars, in the user's own words>`, `[x]` once
     built. A feature that needs more than its line ends with ` → features/F001-<slug>.md`, where the
     detail lives. Nothing is ever nested under a line. -->

- [ ] F001 — <feature the user affirmed — one line, ≤200 chars, nothing beneath it>
- [ ] F002 — <a feature whose detail needed a file of its own> → features/F002-<slug>.md
- [x] F003 — <a feature already built — marked, never removed>
